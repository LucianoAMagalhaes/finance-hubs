node_modules/
.next/
next-env.d.ts
*.tsbuildinfo

# local data: the database and its backup copies never go into git
data/
.env*.local

# the import from the old app runs once and goes away: the script is not app code
importacao/
