node_modules/
.next/
next-env.d.ts
*.tsbuildinfo

# dados locais: o banco e as cópias de segurança nunca vão para o git
dados/
.env*.local

# a importação do app antigo roda uma vez e some: o script não é código do app
importacao/
